USE DeliveryCenter;
GO
-- Análise exploratória
-- Primeiras linhas

SELECT TOP 10 * FROM dbo.channels;
GO

-- Verificação dos valores distintos

SELECT DISTINCT
	channel_name
FROM dbo.channels
ORDER BY channel_name; -- ok, sem erro

SELECT DISTINCT
	channel_type
FROM dbo.channels
ORDER BY channel_type; -- ok, sem erro

-- Valores duplicados
SELECT 
	channel_id, COUNT(*) AS quantidades
FROM dbo.channels
GROUP BY channel_id
HAVING COUNT(*) > 1; -- ok, nenhum valor duplicados
